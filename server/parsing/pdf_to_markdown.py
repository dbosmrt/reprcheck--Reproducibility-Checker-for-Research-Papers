"""
Here I will convert all the pdfs into markdown using marker-pdf library by Datalab.to
"""
import os
import logging
import marker
from marker import markdown

#configuration (Logging)
logging.basicConfig(
    level= INFO,
    format=  ()
)
